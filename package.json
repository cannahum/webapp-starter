{
  "name": "webapp-starter",
  "version": "1.0.0",
  "description": "A Full Stack Web Application Starter with Node, Express, Webpack, React, Scss and TypeScript.",
  "repository": "https://github.com/cannahum/webapp-starter.git",
  "author": "cannahum@outlook.com",
  "license": "MIT",
  "scripts": {
    "setup": "npm install && npm install --prefix app/server && npm install --prefix app/client",
    "setup:clean": "npm install && npm run --prefix app/server setup:clean && npm run --prefix app/client setup:clean",
    "start": "npm start --prefix app/server",
    "server:watch": "npm run --prefix app/server server:watch",
    "start:dev": "npm run --prefix app/server start:dev",
    "test": "npm test --prefix app/client",
    "build:client:dev": "npm run --prefix app/client build:dev",
    "build:client:watch": "npm run --prefix app/client build:watch",
    "build:client:prod": "npm run --prefix app/client build:prod"
  },
  "devDependencies": {
    "tslint": "^5.10.0",
    "typescript": "^2.8.3"
  }
}
